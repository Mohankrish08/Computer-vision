# Computer-vision

1. #### Virtual Mouse
By using Mediapipe and PyAutoGui, track the movements of the hands to control the mouse. 

2. #### Siamese networks
Siamese neural network is a deep learning model that uses two identical subnetworks: one for encoding (encoding) an image into a feature vector while. 
    1. MNIST data by using Siamese networks.

3. #### ANPR
This is about Automatic Number Recognition by using YOLOv8 and Comparision between OCR of EasyOCR and PyTesseract and how it performs.